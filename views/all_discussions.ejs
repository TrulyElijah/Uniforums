<!DOCTYPE html>
<html lang="en"> 
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../css/all_discussions.css">
    <title>Uniforums | <%= title %></title>
</head>
<body>

    <%- include('./partials/navbar.ejs') %>
    
    <main>
        <div class="search-container">
            <input type="text" placeholder="Search">
            <button>Search</button>
        </div>
        <ol class ="discussion-posts">

            <% if (discussions.length > 0) { %>
                <% discussions.forEach(discussion => { %>
                <a href=""><button type="button"></button><li> <%= discussion.title %> </li></button></a>
                <!-- Somehow bring this to a single discussion page -->
                <!-- Use id of discussion post as url -->
            
                <% }) %>
            <% }else {%>
                <p> There are no discussions to display...</p>
                <%  }%>

        </ol>
    </main>
</body>
</html>